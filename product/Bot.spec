# -*- mode: python ; coding: utf-8 -*-
added_files=[
('./../sample','sample'),
('./../screenshot','screenshot'),
('./../output','output'),
('./../mission button','mission button'),
('./../images','images'),
('./../frontend/dist/','frontend')]

a = Analysis(
    ['..\\src\\Bot.py'],
    pathex=['..\\src'],
    binaries=[],
    datas=added_files,
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='ZZZ Bot',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='../images/Qingyi02.ico',
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='ZZZ Bot',
)
